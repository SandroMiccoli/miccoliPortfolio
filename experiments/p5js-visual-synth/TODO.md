# Features

- When DEBUG/SYSTEM is active, it should appear on the top of the page, over everything, so you can see how the system is performing.
- Na tela de CONTROLE (na UI acessada via o celular) deve ter um preview de como a imagem está saindo
- Deve ter um controle de saída de "MAPPING" que no primeiro momento só vai ter a funcionalidade de CORNER PIN (mas depois poderá WARP e BEZIER)
- Nessa saída de mapping vai ser possível ter uma criação de máscaras, assím é possível criar elementos e ir adicionando eles para criar uma máscara complexa (vários círculos, retângulos e posteriormente formas complexas feitas com bezier e tudo)
- TAP BPM (done)
- Definir efeitos a partir de uma "TIMELINE" ou de um "BPM SYNC", igual no Resolume (done: Speed / BPM / FFT per parameter)
- Cada Operador deverá ter vários tipos de PRESETS, que poderão ser salvos
- 
